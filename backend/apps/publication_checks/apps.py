from django.apps import AppConfig


class PublicationChecksConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.publication_checks"
    verbose_name = "Publication verification"
