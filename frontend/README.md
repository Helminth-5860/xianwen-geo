# 前端开发

技术栈：Next.js、TypeScript、Ant Design。

```powershell
npm install
npm run dev
```

质量检查：

```powershell
npm run lint
npm run format:check
npm run typecheck
npm test
npm run build
```

所有依赖使用精确版本，并通过 `package-lock.json` 固定完整依赖树。
